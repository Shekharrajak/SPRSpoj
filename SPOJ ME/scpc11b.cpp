#include <cstdio>
#include<iostream>
#include <algorithm>
using namespace std;

int main() {
	int n, i;
	scanf("%d\n", &n);
	//bool c = b==1;
	
/*	while(scanf("%d", &n) && n) {
		{
		
		for(i = 0; i < n; i++) scanf("%d", a + i);
		sort(a, a + n);
		for(i = 1; i < n; i++) {
			if(a[i] - a[i-1] > 200)
				 break;
		}
		if(i < n || (1422 - a[i-1])*2 > 200) printf("IMPOSSIBLE");
		else printf("POSSIBLE");
		}
	}
	*/
	return 0;
}





